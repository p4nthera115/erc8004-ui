# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Generated llms docs (rebuilt by prebuild script)
public/llms/
public/llms-full.txt

# MCP registry snapshot (rebuilt by the same script)
packages/mcp-server/src/generated/

# Environment variables
.env
.env.*
# api/_generated/registry.ts is deliberately NOT ignored: the Vercel functions
# under /api import it, and Vercel does not guarantee they are built after the
# build command that generates it.
